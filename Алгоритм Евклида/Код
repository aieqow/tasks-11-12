def gcd(a, b):
    # Базовый случай: если b равно 0, то НОД = a
    if b == 0:
        return a
    # Рекурсивный случай: НОД(a, b) = НОД(b, a % b)
    return gcd(b, a % b)
# Пример использования:
print(gcd(49, 70))  # Результат: 7
